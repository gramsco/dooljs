{
  "name": "@gramsco/days",
  "version": "0.0.1",
  "description": "a package to help generate list of days based on a simple rule",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "dates",
    "calendar"
  ],
  "author": "gramsco",
  "license": "ISC",
  "dependencies": {
    "dayjs": "^1.10.4"
  }
}
